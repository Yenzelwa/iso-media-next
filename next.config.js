

const nextConfig = {
  output: 'standalone', 
  reactStrictMode: true,
  trailingSlash: true,
  images: {
    domains: ['images.unsplash.com'],
    unoptimized: false, 
  },
};

module.exports = nextConfig;
