

const nextConfig = {
  output: 'export', 
  reactStrictMode: true,
  trailingSlash: true,
  images: {
    domains: ['images.unsplash.com'],
    unoptimized: true, 
  },
};

module.exports = nextConfig;
