import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
    title: 'isolakwamuntu login',
    description: 'Browse all categories',
  }
const LoginPage = () =>{

    return (
        <>
        <h3>Login page!</h3>
        </>
    )
}

export default LoginPage;